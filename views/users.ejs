<!DOCTYPE html>
<html>
<head>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
  <h2><%= title %></h2>
</head>
<style>

img {
  width:50px;
  height: 30px;
}

.card {
         padding: 10px;
         padding-top: 20px;
         background: #18bc9c;
         border: 1px #c8c3ff solid;
         border-radius: 5px;
         text-align: center;
         margin: 10px 3% 1% 3%;
         height: 190px;
         width: calc(80% * (1/3));
     }
     .card .card-title {
         color: #02695e;
         font-size: 2em;
         font-weight: bold;
         text-transform: capitalize;
     }
     .card .card-link {
         margin-top: 40px;
         font-size: 1.1em;
         font-weight: bold;
         width: 90%;
         border: 2px solid white;
         color: white;
         background: transparent;
         transition: background-color 0.5s ease;
     }

</style>
<body>
  <div class="row">
    <hr>
      <% for(var i=0; i<users.length; i++){ %>
      <div class="card col-xs-4">
        <h4 class="card-title"> <%= users[i].name %></h4>
        <img src=<%= users[i].profile_pic %>>
        <a href=<%= "/users/"+users[i].name%> class="btn btn-primary card-link">Visit Profile</a>
      </div>
      <% } %>
  </div>
</body>
</html>
