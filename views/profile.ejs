<!DOCTYPE html>
<html>
<head>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
  <img src= <%= profile_pic %> >
  <style>

  .row img {
      width:150px;
      height:150px;

  }

  .card {
           padding: 10px;
           padding-top: 20px;
           background: #18bc9c;
           border: 1px #c8c3ff solid;
           border-radius: 5px;
           text-align: center;
           margin: 10px 3% 1% 3%;
           height: 190px;
           width: calc(80% * (1/3));
       }
       .card .card-title {
           color: #02695e;
           font-size: 2em;
           font-weight: bold;
           text-transform: capitalize;
       }
       .card .card-link {
           margin-top: 40px;
           font-size: 1.1em;
           font-weight: bold;
           width: 90%;
           border: 2px solid white;
           color: white;
           background: transparent;
           transition: background-color 0.5s ease;
       }
  </style>
</head>
<body>
  <br>
  <br>
  <form method="post" action="/addwork" >
    <h1><%= title %><h1>
    <p>Your Email is <%= email%> </p>
    <h3>Add Work</h3>
    <input name="work" class="form-control" type="text" placeholder="Add Work here .." class="input pass"/>
    <hr>
    <input type="submit" class="btn btn-default" value="submit work" class="inputButton"/>
    <hr>
  </form>
  <div class="row">
      <% for(var i=0; i<users.work.length; i++){ %>
      <div class="card col-xs-4">
        <h4 class="card-title"> <%= users.work[i] %></h4>
      </div>
      <% } %>
  </div>
  <hr>
  <h3>Add Screenshot</h3>
  <form method="post" action="/addscreenshot" >
      <input name="screenshot" class="form-control" type="text" placeholder="Add screenshot links here .." class="input pass"/>
      <hr>
      <input type="submit" class="btn btn-default" value="submit screenshot" class="inputButton"/>
      <hr>
    </form>
    <div class="row">
        <% for(var i=0; i<users.screenshot.length; i++){ %>
        <div class="card col-xs-4">
          <img src= <%= users.screenshot[i] %> >
        </div>
        <% } %>
    </div>
    <hr>
  <a href="/users" class="btn btn-primary card-link">View Users</a>
</body>
</html>
